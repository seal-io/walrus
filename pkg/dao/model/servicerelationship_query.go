// SPDX-FileCopyrightText: 2023 Seal, Inc
// SPDX-License-Identifier: Apache-2.0

// Code generated by "seal". DO NOT EDIT.

package model

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"

	"github.com/seal-io/seal/pkg/dao/model/internal"
	"github.com/seal-io/seal/pkg/dao/model/predicate"
	"github.com/seal-io/seal/pkg/dao/model/service"
	"github.com/seal-io/seal/pkg/dao/model/servicerelationship"
	"github.com/seal-io/seal/pkg/dao/types/object"
)

// ServiceRelationshipQuery is the builder for querying ServiceRelationship entities.
type ServiceRelationshipQuery struct {
	config
	ctx            *QueryContext
	order          []servicerelationship.OrderOption
	inters         []Interceptor
	predicates     []predicate.ServiceRelationship
	withService    *ServiceQuery
	withDependency *ServiceQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ServiceRelationshipQuery builder.
func (srq *ServiceRelationshipQuery) Where(ps ...predicate.ServiceRelationship) *ServiceRelationshipQuery {
	srq.predicates = append(srq.predicates, ps...)
	return srq
}

// Limit the number of records to be returned by this query.
func (srq *ServiceRelationshipQuery) Limit(limit int) *ServiceRelationshipQuery {
	srq.ctx.Limit = &limit
	return srq
}

// Offset to start from.
func (srq *ServiceRelationshipQuery) Offset(offset int) *ServiceRelationshipQuery {
	srq.ctx.Offset = &offset
	return srq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (srq *ServiceRelationshipQuery) Unique(unique bool) *ServiceRelationshipQuery {
	srq.ctx.Unique = &unique
	return srq
}

// Order specifies how the records should be ordered.
func (srq *ServiceRelationshipQuery) Order(o ...servicerelationship.OrderOption) *ServiceRelationshipQuery {
	srq.order = append(srq.order, o...)
	return srq
}

// QueryService chains the current query on the "service" edge.
func (srq *ServiceRelationshipQuery) QueryService() *ServiceQuery {
	query := (&ServiceClient{config: srq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(servicerelationship.Table, servicerelationship.FieldID, selector),
			sqlgraph.To(service.Table, service.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, servicerelationship.ServiceTable, servicerelationship.ServiceColumn),
		)
		schemaConfig := srq.schemaConfig
		step.To.Schema = schemaConfig.Service
		step.Edge.Schema = schemaConfig.ServiceRelationship
		fromU = sqlgraph.SetNeighbors(srq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDependency chains the current query on the "dependency" edge.
func (srq *ServiceRelationshipQuery) QueryDependency() *ServiceQuery {
	query := (&ServiceClient{config: srq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(servicerelationship.Table, servicerelationship.FieldID, selector),
			sqlgraph.To(service.Table, service.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, servicerelationship.DependencyTable, servicerelationship.DependencyColumn),
		)
		schemaConfig := srq.schemaConfig
		step.To.Schema = schemaConfig.Service
		step.Edge.Schema = schemaConfig.ServiceRelationship
		fromU = sqlgraph.SetNeighbors(srq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ServiceRelationship entity from the query.
// Returns a *NotFoundError when no ServiceRelationship was found.
func (srq *ServiceRelationshipQuery) First(ctx context.Context) (*ServiceRelationship, error) {
	nodes, err := srq.Limit(1).All(setContextOp(ctx, srq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{servicerelationship.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (srq *ServiceRelationshipQuery) FirstX(ctx context.Context) *ServiceRelationship {
	node, err := srq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ServiceRelationship ID from the query.
// Returns a *NotFoundError when no ServiceRelationship ID was found.
func (srq *ServiceRelationshipQuery) FirstID(ctx context.Context) (id object.ID, err error) {
	var ids []object.ID
	if ids, err = srq.Limit(1).IDs(setContextOp(ctx, srq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{servicerelationship.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (srq *ServiceRelationshipQuery) FirstIDX(ctx context.Context) object.ID {
	id, err := srq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ServiceRelationship entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ServiceRelationship entity is found.
// Returns a *NotFoundError when no ServiceRelationship entities are found.
func (srq *ServiceRelationshipQuery) Only(ctx context.Context) (*ServiceRelationship, error) {
	nodes, err := srq.Limit(2).All(setContextOp(ctx, srq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{servicerelationship.Label}
	default:
		return nil, &NotSingularError{servicerelationship.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (srq *ServiceRelationshipQuery) OnlyX(ctx context.Context) *ServiceRelationship {
	node, err := srq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ServiceRelationship ID in the query.
// Returns a *NotSingularError when more than one ServiceRelationship ID is found.
// Returns a *NotFoundError when no entities are found.
func (srq *ServiceRelationshipQuery) OnlyID(ctx context.Context) (id object.ID, err error) {
	var ids []object.ID
	if ids, err = srq.Limit(2).IDs(setContextOp(ctx, srq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{servicerelationship.Label}
	default:
		err = &NotSingularError{servicerelationship.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (srq *ServiceRelationshipQuery) OnlyIDX(ctx context.Context) object.ID {
	id, err := srq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ServiceRelationships.
func (srq *ServiceRelationshipQuery) All(ctx context.Context) ([]*ServiceRelationship, error) {
	ctx = setContextOp(ctx, srq.ctx, "All")
	if err := srq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ServiceRelationship, *ServiceRelationshipQuery]()
	return withInterceptors[[]*ServiceRelationship](ctx, srq, qr, srq.inters)
}

// AllX is like All, but panics if an error occurs.
func (srq *ServiceRelationshipQuery) AllX(ctx context.Context) []*ServiceRelationship {
	nodes, err := srq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ServiceRelationship IDs.
func (srq *ServiceRelationshipQuery) IDs(ctx context.Context) (ids []object.ID, err error) {
	if srq.ctx.Unique == nil && srq.path != nil {
		srq.Unique(true)
	}
	ctx = setContextOp(ctx, srq.ctx, "IDs")
	if err = srq.Select(servicerelationship.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (srq *ServiceRelationshipQuery) IDsX(ctx context.Context) []object.ID {
	ids, err := srq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (srq *ServiceRelationshipQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, srq.ctx, "Count")
	if err := srq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, srq, querierCount[*ServiceRelationshipQuery](), srq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (srq *ServiceRelationshipQuery) CountX(ctx context.Context) int {
	count, err := srq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (srq *ServiceRelationshipQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, srq.ctx, "Exist")
	switch _, err := srq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("model: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (srq *ServiceRelationshipQuery) ExistX(ctx context.Context) bool {
	exist, err := srq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ServiceRelationshipQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (srq *ServiceRelationshipQuery) Clone() *ServiceRelationshipQuery {
	if srq == nil {
		return nil
	}
	return &ServiceRelationshipQuery{
		config:         srq.config,
		ctx:            srq.ctx.Clone(),
		order:          append([]servicerelationship.OrderOption{}, srq.order...),
		inters:         append([]Interceptor{}, srq.inters...),
		predicates:     append([]predicate.ServiceRelationship{}, srq.predicates...),
		withService:    srq.withService.Clone(),
		withDependency: srq.withDependency.Clone(),
		// clone intermediate query.
		sql:  srq.sql.Clone(),
		path: srq.path,
	}
}

// WithService tells the query-builder to eager-load the nodes that are connected to
// the "service" edge. The optional arguments are used to configure the query builder of the edge.
func (srq *ServiceRelationshipQuery) WithService(opts ...func(*ServiceQuery)) *ServiceRelationshipQuery {
	query := (&ServiceClient{config: srq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srq.withService = query
	return srq
}

// WithDependency tells the query-builder to eager-load the nodes that are connected to
// the "dependency" edge. The optional arguments are used to configure the query builder of the edge.
func (srq *ServiceRelationshipQuery) WithDependency(opts ...func(*ServiceQuery)) *ServiceRelationshipQuery {
	query := (&ServiceClient{config: srq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srq.withDependency = query
	return srq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ServiceRelationship.Query().
//		GroupBy(servicerelationship.FieldCreateTime).
//		Aggregate(model.Count()).
//		Scan(ctx, &v)
func (srq *ServiceRelationshipQuery) GroupBy(field string, fields ...string) *ServiceRelationshipGroupBy {
	srq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ServiceRelationshipGroupBy{build: srq}
	grbuild.flds = &srq.ctx.Fields
	grbuild.label = servicerelationship.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.ServiceRelationship.Query().
//		Select(servicerelationship.FieldCreateTime).
//		Scan(ctx, &v)
func (srq *ServiceRelationshipQuery) Select(fields ...string) *ServiceRelationshipSelect {
	srq.ctx.Fields = append(srq.ctx.Fields, fields...)
	sbuild := &ServiceRelationshipSelect{ServiceRelationshipQuery: srq}
	sbuild.label = servicerelationship.Label
	sbuild.flds, sbuild.scan = &srq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ServiceRelationshipSelect configured with the given aggregations.
func (srq *ServiceRelationshipQuery) Aggregate(fns ...AggregateFunc) *ServiceRelationshipSelect {
	return srq.Select().Aggregate(fns...)
}

func (srq *ServiceRelationshipQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range srq.inters {
		if inter == nil {
			return fmt.Errorf("model: uninitialized interceptor (forgotten import model/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, srq); err != nil {
				return err
			}
		}
	}
	for _, f := range srq.ctx.Fields {
		if !servicerelationship.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("model: invalid field %q for query", f)}
		}
	}
	if srq.path != nil {
		prev, err := srq.path(ctx)
		if err != nil {
			return err
		}
		srq.sql = prev
	}
	return nil
}

func (srq *ServiceRelationshipQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ServiceRelationship, error) {
	var (
		nodes       = []*ServiceRelationship{}
		_spec       = srq.querySpec()
		loadedTypes = [2]bool{
			srq.withService != nil,
			srq.withDependency != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ServiceRelationship).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ServiceRelationship{config: srq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = srq.schemaConfig.ServiceRelationship
	ctx = internal.NewSchemaConfigContext(ctx, srq.schemaConfig)
	if len(srq.modifiers) > 0 {
		_spec.Modifiers = srq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, srq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := srq.withService; query != nil {
		if err := srq.loadService(ctx, query, nodes, nil,
			func(n *ServiceRelationship, e *Service) { n.Edges.Service = e }); err != nil {
			return nil, err
		}
	}
	if query := srq.withDependency; query != nil {
		if err := srq.loadDependency(ctx, query, nodes, nil,
			func(n *ServiceRelationship, e *Service) { n.Edges.Dependency = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (srq *ServiceRelationshipQuery) loadService(ctx context.Context, query *ServiceQuery, nodes []*ServiceRelationship, init func(*ServiceRelationship), assign func(*ServiceRelationship, *Service)) error {
	ids := make([]object.ID, 0, len(nodes))
	nodeids := make(map[object.ID][]*ServiceRelationship)
	for i := range nodes {
		fk := nodes[i].ServiceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(service.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "service_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (srq *ServiceRelationshipQuery) loadDependency(ctx context.Context, query *ServiceQuery, nodes []*ServiceRelationship, init func(*ServiceRelationship), assign func(*ServiceRelationship, *Service)) error {
	ids := make([]object.ID, 0, len(nodes))
	nodeids := make(map[object.ID][]*ServiceRelationship)
	for i := range nodes {
		fk := nodes[i].DependencyID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(service.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "dependency_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (srq *ServiceRelationshipQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := srq.querySpec()
	_spec.Node.Schema = srq.schemaConfig.ServiceRelationship
	ctx = internal.NewSchemaConfigContext(ctx, srq.schemaConfig)
	if len(srq.modifiers) > 0 {
		_spec.Modifiers = srq.modifiers
	}
	_spec.Node.Columns = srq.ctx.Fields
	if len(srq.ctx.Fields) > 0 {
		_spec.Unique = srq.ctx.Unique != nil && *srq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, srq.driver, _spec)
}

func (srq *ServiceRelationshipQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(servicerelationship.Table, servicerelationship.Columns, sqlgraph.NewFieldSpec(servicerelationship.FieldID, field.TypeString))
	_spec.From = srq.sql
	if unique := srq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if srq.path != nil {
		_spec.Unique = true
	}
	if fields := srq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, servicerelationship.FieldID)
		for i := range fields {
			if fields[i] != servicerelationship.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if srq.withService != nil {
			_spec.Node.AddColumnOnce(servicerelationship.FieldServiceID)
		}
		if srq.withDependency != nil {
			_spec.Node.AddColumnOnce(servicerelationship.FieldDependencyID)
		}
	}
	if ps := srq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := srq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := srq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := srq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (srq *ServiceRelationshipQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(srq.driver.Dialect())
	t1 := builder.Table(servicerelationship.Table)
	columns := srq.ctx.Fields
	if len(columns) == 0 {
		columns = servicerelationship.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if srq.sql != nil {
		selector = srq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if srq.ctx.Unique != nil && *srq.ctx.Unique {
		selector.Distinct()
	}
	t1.Schema(srq.schemaConfig.ServiceRelationship)
	ctx = internal.NewSchemaConfigContext(ctx, srq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range srq.modifiers {
		m(selector)
	}
	for _, p := range srq.predicates {
		p(selector)
	}
	for _, p := range srq.order {
		p(selector)
	}
	if offset := srq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := srq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (srq *ServiceRelationshipQuery) ForUpdate(opts ...sql.LockOption) *ServiceRelationshipQuery {
	if srq.driver.Dialect() == dialect.Postgres {
		srq.Unique(false)
	}
	srq.modifiers = append(srq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return srq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (srq *ServiceRelationshipQuery) ForShare(opts ...sql.LockOption) *ServiceRelationshipQuery {
	if srq.driver.Dialect() == dialect.Postgres {
		srq.Unique(false)
	}
	srq.modifiers = append(srq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return srq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (srq *ServiceRelationshipQuery) Modify(modifiers ...func(s *sql.Selector)) *ServiceRelationshipSelect {
	srq.modifiers = append(srq.modifiers, modifiers...)
	return srq.Select()
}

// WhereP appends storage-level predicates to the ServiceRelationshipQuery builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (srq *ServiceRelationshipQuery) WhereP(ps ...func(*sql.Selector)) {
	var wps = make([]predicate.ServiceRelationship, 0, len(ps))
	for i := 0; i < len(ps); i++ {
		wps = append(wps, predicate.ServiceRelationship(ps[i]))
	}
	srq.predicates = append(srq.predicates, wps...)
}

// ServiceRelationshipGroupBy is the group-by builder for ServiceRelationship entities.
type ServiceRelationshipGroupBy struct {
	selector
	build *ServiceRelationshipQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (srgb *ServiceRelationshipGroupBy) Aggregate(fns ...AggregateFunc) *ServiceRelationshipGroupBy {
	srgb.fns = append(srgb.fns, fns...)
	return srgb
}

// Scan applies the selector query and scans the result into the given value.
func (srgb *ServiceRelationshipGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srgb.build.ctx, "GroupBy")
	if err := srgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServiceRelationshipQuery, *ServiceRelationshipGroupBy](ctx, srgb.build, srgb, srgb.build.inters, v)
}

func (srgb *ServiceRelationshipGroupBy) sqlScan(ctx context.Context, root *ServiceRelationshipQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(srgb.fns))
	for _, fn := range srgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*srgb.flds)+len(srgb.fns))
		for _, f := range *srgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*srgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ServiceRelationshipSelect is the builder for selecting fields of ServiceRelationship entities.
type ServiceRelationshipSelect struct {
	*ServiceRelationshipQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (srs *ServiceRelationshipSelect) Aggregate(fns ...AggregateFunc) *ServiceRelationshipSelect {
	srs.fns = append(srs.fns, fns...)
	return srs
}

// Scan applies the selector query and scans the result into the given value.
func (srs *ServiceRelationshipSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srs.ctx, "Select")
	if err := srs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServiceRelationshipQuery, *ServiceRelationshipSelect](ctx, srs.ServiceRelationshipQuery, srs, srs.inters, v)
}

func (srs *ServiceRelationshipSelect) sqlScan(ctx context.Context, root *ServiceRelationshipQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(srs.fns))
	for _, fn := range srs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*srs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (srs *ServiceRelationshipSelect) Modify(modifiers ...func(s *sql.Selector)) *ServiceRelationshipSelect {
	srs.modifiers = append(srs.modifiers, modifiers...)
	return srs
}
