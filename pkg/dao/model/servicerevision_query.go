// SPDX-FileCopyrightText: 2023 Seal, Inc
// SPDX-License-Identifier: Apache-2.0

// Code generated by "ent". DO NOT EDIT.

package model

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"

	"github.com/seal-io/seal/pkg/dao/model/environment"
	"github.com/seal-io/seal/pkg/dao/model/internal"
	"github.com/seal-io/seal/pkg/dao/model/predicate"
	"github.com/seal-io/seal/pkg/dao/model/project"
	"github.com/seal-io/seal/pkg/dao/model/service"
	"github.com/seal-io/seal/pkg/dao/model/servicerevision"
	"github.com/seal-io/seal/pkg/dao/types/object"
)

// ServiceRevisionQuery is the builder for querying ServiceRevision entities.
type ServiceRevisionQuery struct {
	config
	ctx             *QueryContext
	order           []servicerevision.OrderOption
	inters          []Interceptor
	predicates      []predicate.ServiceRevision
	withProject     *ProjectQuery
	withEnvironment *EnvironmentQuery
	withService     *ServiceQuery
	modifiers       []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ServiceRevisionQuery builder.
func (srq *ServiceRevisionQuery) Where(ps ...predicate.ServiceRevision) *ServiceRevisionQuery {
	srq.predicates = append(srq.predicates, ps...)
	return srq
}

// Limit the number of records to be returned by this query.
func (srq *ServiceRevisionQuery) Limit(limit int) *ServiceRevisionQuery {
	srq.ctx.Limit = &limit
	return srq
}

// Offset to start from.
func (srq *ServiceRevisionQuery) Offset(offset int) *ServiceRevisionQuery {
	srq.ctx.Offset = &offset
	return srq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (srq *ServiceRevisionQuery) Unique(unique bool) *ServiceRevisionQuery {
	srq.ctx.Unique = &unique
	return srq
}

// Order specifies how the records should be ordered.
func (srq *ServiceRevisionQuery) Order(o ...servicerevision.OrderOption) *ServiceRevisionQuery {
	srq.order = append(srq.order, o...)
	return srq
}

// QueryProject chains the current query on the "project" edge.
func (srq *ServiceRevisionQuery) QueryProject() *ProjectQuery {
	query := (&ProjectClient{config: srq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(servicerevision.Table, servicerevision.FieldID, selector),
			sqlgraph.To(project.Table, project.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, servicerevision.ProjectTable, servicerevision.ProjectColumn),
		)
		schemaConfig := srq.schemaConfig
		step.To.Schema = schemaConfig.Project
		step.Edge.Schema = schemaConfig.ServiceRevision
		fromU = sqlgraph.SetNeighbors(srq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEnvironment chains the current query on the "environment" edge.
func (srq *ServiceRevisionQuery) QueryEnvironment() *EnvironmentQuery {
	query := (&EnvironmentClient{config: srq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(servicerevision.Table, servicerevision.FieldID, selector),
			sqlgraph.To(environment.Table, environment.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, servicerevision.EnvironmentTable, servicerevision.EnvironmentColumn),
		)
		schemaConfig := srq.schemaConfig
		step.To.Schema = schemaConfig.Environment
		step.Edge.Schema = schemaConfig.ServiceRevision
		fromU = sqlgraph.SetNeighbors(srq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryService chains the current query on the "service" edge.
func (srq *ServiceRevisionQuery) QueryService() *ServiceQuery {
	query := (&ServiceClient{config: srq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(servicerevision.Table, servicerevision.FieldID, selector),
			sqlgraph.To(service.Table, service.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, servicerevision.ServiceTable, servicerevision.ServiceColumn),
		)
		schemaConfig := srq.schemaConfig
		step.To.Schema = schemaConfig.Service
		step.Edge.Schema = schemaConfig.ServiceRevision
		fromU = sqlgraph.SetNeighbors(srq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ServiceRevision entity from the query.
// Returns a *NotFoundError when no ServiceRevision was found.
func (srq *ServiceRevisionQuery) First(ctx context.Context) (*ServiceRevision, error) {
	nodes, err := srq.Limit(1).All(setContextOp(ctx, srq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{servicerevision.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (srq *ServiceRevisionQuery) FirstX(ctx context.Context) *ServiceRevision {
	node, err := srq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ServiceRevision ID from the query.
// Returns a *NotFoundError when no ServiceRevision ID was found.
func (srq *ServiceRevisionQuery) FirstID(ctx context.Context) (id object.ID, err error) {
	var ids []object.ID
	if ids, err = srq.Limit(1).IDs(setContextOp(ctx, srq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{servicerevision.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (srq *ServiceRevisionQuery) FirstIDX(ctx context.Context) object.ID {
	id, err := srq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ServiceRevision entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ServiceRevision entity is found.
// Returns a *NotFoundError when no ServiceRevision entities are found.
func (srq *ServiceRevisionQuery) Only(ctx context.Context) (*ServiceRevision, error) {
	nodes, err := srq.Limit(2).All(setContextOp(ctx, srq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{servicerevision.Label}
	default:
		return nil, &NotSingularError{servicerevision.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (srq *ServiceRevisionQuery) OnlyX(ctx context.Context) *ServiceRevision {
	node, err := srq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ServiceRevision ID in the query.
// Returns a *NotSingularError when more than one ServiceRevision ID is found.
// Returns a *NotFoundError when no entities are found.
func (srq *ServiceRevisionQuery) OnlyID(ctx context.Context) (id object.ID, err error) {
	var ids []object.ID
	if ids, err = srq.Limit(2).IDs(setContextOp(ctx, srq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{servicerevision.Label}
	default:
		err = &NotSingularError{servicerevision.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (srq *ServiceRevisionQuery) OnlyIDX(ctx context.Context) object.ID {
	id, err := srq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ServiceRevisions.
func (srq *ServiceRevisionQuery) All(ctx context.Context) ([]*ServiceRevision, error) {
	ctx = setContextOp(ctx, srq.ctx, "All")
	if err := srq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ServiceRevision, *ServiceRevisionQuery]()
	return withInterceptors[[]*ServiceRevision](ctx, srq, qr, srq.inters)
}

// AllX is like All, but panics if an error occurs.
func (srq *ServiceRevisionQuery) AllX(ctx context.Context) []*ServiceRevision {
	nodes, err := srq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ServiceRevision IDs.
func (srq *ServiceRevisionQuery) IDs(ctx context.Context) (ids []object.ID, err error) {
	if srq.ctx.Unique == nil && srq.path != nil {
		srq.Unique(true)
	}
	ctx = setContextOp(ctx, srq.ctx, "IDs")
	if err = srq.Select(servicerevision.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (srq *ServiceRevisionQuery) IDsX(ctx context.Context) []object.ID {
	ids, err := srq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (srq *ServiceRevisionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, srq.ctx, "Count")
	if err := srq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, srq, querierCount[*ServiceRevisionQuery](), srq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (srq *ServiceRevisionQuery) CountX(ctx context.Context) int {
	count, err := srq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (srq *ServiceRevisionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, srq.ctx, "Exist")
	switch _, err := srq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("model: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (srq *ServiceRevisionQuery) ExistX(ctx context.Context) bool {
	exist, err := srq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ServiceRevisionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (srq *ServiceRevisionQuery) Clone() *ServiceRevisionQuery {
	if srq == nil {
		return nil
	}
	return &ServiceRevisionQuery{
		config:          srq.config,
		ctx:             srq.ctx.Clone(),
		order:           append([]servicerevision.OrderOption{}, srq.order...),
		inters:          append([]Interceptor{}, srq.inters...),
		predicates:      append([]predicate.ServiceRevision{}, srq.predicates...),
		withProject:     srq.withProject.Clone(),
		withEnvironment: srq.withEnvironment.Clone(),
		withService:     srq.withService.Clone(),
		// clone intermediate query.
		sql:  srq.sql.Clone(),
		path: srq.path,
	}
}

// WithProject tells the query-builder to eager-load the nodes that are connected to
// the "project" edge. The optional arguments are used to configure the query builder of the edge.
func (srq *ServiceRevisionQuery) WithProject(opts ...func(*ProjectQuery)) *ServiceRevisionQuery {
	query := (&ProjectClient{config: srq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srq.withProject = query
	return srq
}

// WithEnvironment tells the query-builder to eager-load the nodes that are connected to
// the "environment" edge. The optional arguments are used to configure the query builder of the edge.
func (srq *ServiceRevisionQuery) WithEnvironment(opts ...func(*EnvironmentQuery)) *ServiceRevisionQuery {
	query := (&EnvironmentClient{config: srq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srq.withEnvironment = query
	return srq
}

// WithService tells the query-builder to eager-load the nodes that are connected to
// the "service" edge. The optional arguments are used to configure the query builder of the edge.
func (srq *ServiceRevisionQuery) WithService(opts ...func(*ServiceQuery)) *ServiceRevisionQuery {
	query := (&ServiceClient{config: srq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srq.withService = query
	return srq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"createTime,omitempty" sql:"createTime"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ServiceRevision.Query().
//		GroupBy(servicerevision.FieldCreateTime).
//		Aggregate(model.Count()).
//		Scan(ctx, &v)
func (srq *ServiceRevisionQuery) GroupBy(field string, fields ...string) *ServiceRevisionGroupBy {
	srq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ServiceRevisionGroupBy{build: srq}
	grbuild.flds = &srq.ctx.Fields
	grbuild.label = servicerevision.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"createTime,omitempty" sql:"createTime"`
//	}
//
//	client.ServiceRevision.Query().
//		Select(servicerevision.FieldCreateTime).
//		Scan(ctx, &v)
func (srq *ServiceRevisionQuery) Select(fields ...string) *ServiceRevisionSelect {
	srq.ctx.Fields = append(srq.ctx.Fields, fields...)
	sbuild := &ServiceRevisionSelect{ServiceRevisionQuery: srq}
	sbuild.label = servicerevision.Label
	sbuild.flds, sbuild.scan = &srq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ServiceRevisionSelect configured with the given aggregations.
func (srq *ServiceRevisionQuery) Aggregate(fns ...AggregateFunc) *ServiceRevisionSelect {
	return srq.Select().Aggregate(fns...)
}

func (srq *ServiceRevisionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range srq.inters {
		if inter == nil {
			return fmt.Errorf("model: uninitialized interceptor (forgotten import model/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, srq); err != nil {
				return err
			}
		}
	}
	for _, f := range srq.ctx.Fields {
		if !servicerevision.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("model: invalid field %q for query", f)}
		}
	}
	if srq.path != nil {
		prev, err := srq.path(ctx)
		if err != nil {
			return err
		}
		srq.sql = prev
	}
	return nil
}

func (srq *ServiceRevisionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ServiceRevision, error) {
	var (
		nodes       = []*ServiceRevision{}
		_spec       = srq.querySpec()
		loadedTypes = [3]bool{
			srq.withProject != nil,
			srq.withEnvironment != nil,
			srq.withService != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ServiceRevision).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ServiceRevision{config: srq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = srq.schemaConfig.ServiceRevision
	ctx = internal.NewSchemaConfigContext(ctx, srq.schemaConfig)
	if len(srq.modifiers) > 0 {
		_spec.Modifiers = srq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, srq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := srq.withProject; query != nil {
		if err := srq.loadProject(ctx, query, nodes, nil,
			func(n *ServiceRevision, e *Project) { n.Edges.Project = e }); err != nil {
			return nil, err
		}
	}
	if query := srq.withEnvironment; query != nil {
		if err := srq.loadEnvironment(ctx, query, nodes, nil,
			func(n *ServiceRevision, e *Environment) { n.Edges.Environment = e }); err != nil {
			return nil, err
		}
	}
	if query := srq.withService; query != nil {
		if err := srq.loadService(ctx, query, nodes, nil,
			func(n *ServiceRevision, e *Service) { n.Edges.Service = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (srq *ServiceRevisionQuery) loadProject(ctx context.Context, query *ProjectQuery, nodes []*ServiceRevision, init func(*ServiceRevision), assign func(*ServiceRevision, *Project)) error {
	ids := make([]object.ID, 0, len(nodes))
	nodeids := make(map[object.ID][]*ServiceRevision)
	for i := range nodes {
		fk := nodes[i].ProjectID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(project.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "projectID" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (srq *ServiceRevisionQuery) loadEnvironment(ctx context.Context, query *EnvironmentQuery, nodes []*ServiceRevision, init func(*ServiceRevision), assign func(*ServiceRevision, *Environment)) error {
	ids := make([]object.ID, 0, len(nodes))
	nodeids := make(map[object.ID][]*ServiceRevision)
	for i := range nodes {
		fk := nodes[i].EnvironmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(environment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "environmentID" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (srq *ServiceRevisionQuery) loadService(ctx context.Context, query *ServiceQuery, nodes []*ServiceRevision, init func(*ServiceRevision), assign func(*ServiceRevision, *Service)) error {
	ids := make([]object.ID, 0, len(nodes))
	nodeids := make(map[object.ID][]*ServiceRevision)
	for i := range nodes {
		fk := nodes[i].ServiceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(service.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "serviceID" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (srq *ServiceRevisionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := srq.querySpec()
	_spec.Node.Schema = srq.schemaConfig.ServiceRevision
	ctx = internal.NewSchemaConfigContext(ctx, srq.schemaConfig)
	if len(srq.modifiers) > 0 {
		_spec.Modifiers = srq.modifiers
	}
	_spec.Node.Columns = srq.ctx.Fields
	if len(srq.ctx.Fields) > 0 {
		_spec.Unique = srq.ctx.Unique != nil && *srq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, srq.driver, _spec)
}

func (srq *ServiceRevisionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(servicerevision.Table, servicerevision.Columns, sqlgraph.NewFieldSpec(servicerevision.FieldID, field.TypeString))
	_spec.From = srq.sql
	if unique := srq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if srq.path != nil {
		_spec.Unique = true
	}
	if fields := srq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, servicerevision.FieldID)
		for i := range fields {
			if fields[i] != servicerevision.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if srq.withProject != nil {
			_spec.Node.AddColumnOnce(servicerevision.FieldProjectID)
		}
		if srq.withEnvironment != nil {
			_spec.Node.AddColumnOnce(servicerevision.FieldEnvironmentID)
		}
		if srq.withService != nil {
			_spec.Node.AddColumnOnce(servicerevision.FieldServiceID)
		}
	}
	if ps := srq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := srq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := srq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := srq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (srq *ServiceRevisionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(srq.driver.Dialect())
	t1 := builder.Table(servicerevision.Table)
	columns := srq.ctx.Fields
	if len(columns) == 0 {
		columns = servicerevision.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if srq.sql != nil {
		selector = srq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if srq.ctx.Unique != nil && *srq.ctx.Unique {
		selector.Distinct()
	}
	t1.Schema(srq.schemaConfig.ServiceRevision)
	ctx = internal.NewSchemaConfigContext(ctx, srq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range srq.modifiers {
		m(selector)
	}
	for _, p := range srq.predicates {
		p(selector)
	}
	for _, p := range srq.order {
		p(selector)
	}
	if offset := srq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := srq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (srq *ServiceRevisionQuery) ForUpdate(opts ...sql.LockOption) *ServiceRevisionQuery {
	if srq.driver.Dialect() == dialect.Postgres {
		srq.Unique(false)
	}
	srq.modifiers = append(srq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return srq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (srq *ServiceRevisionQuery) ForShare(opts ...sql.LockOption) *ServiceRevisionQuery {
	if srq.driver.Dialect() == dialect.Postgres {
		srq.Unique(false)
	}
	srq.modifiers = append(srq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return srq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (srq *ServiceRevisionQuery) Modify(modifiers ...func(s *sql.Selector)) *ServiceRevisionSelect {
	srq.modifiers = append(srq.modifiers, modifiers...)
	return srq.Select()
}

// WhereP appends storage-level predicates to the ServiceRevisionQuery builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (srq *ServiceRevisionQuery) WhereP(ps ...func(*sql.Selector)) {
	var wps = make([]predicate.ServiceRevision, 0, len(ps))
	for i := 0; i < len(ps); i++ {
		wps = append(wps, predicate.ServiceRevision(ps[i]))
	}
	srq.predicates = append(srq.predicates, wps...)
}

// ServiceRevisionGroupBy is the group-by builder for ServiceRevision entities.
type ServiceRevisionGroupBy struct {
	selector
	build *ServiceRevisionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (srgb *ServiceRevisionGroupBy) Aggregate(fns ...AggregateFunc) *ServiceRevisionGroupBy {
	srgb.fns = append(srgb.fns, fns...)
	return srgb
}

// Scan applies the selector query and scans the result into the given value.
func (srgb *ServiceRevisionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srgb.build.ctx, "GroupBy")
	if err := srgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServiceRevisionQuery, *ServiceRevisionGroupBy](ctx, srgb.build, srgb, srgb.build.inters, v)
}

func (srgb *ServiceRevisionGroupBy) sqlScan(ctx context.Context, root *ServiceRevisionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(srgb.fns))
	for _, fn := range srgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*srgb.flds)+len(srgb.fns))
		for _, f := range *srgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*srgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ServiceRevisionSelect is the builder for selecting fields of ServiceRevision entities.
type ServiceRevisionSelect struct {
	*ServiceRevisionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (srs *ServiceRevisionSelect) Aggregate(fns ...AggregateFunc) *ServiceRevisionSelect {
	srs.fns = append(srs.fns, fns...)
	return srs
}

// Scan applies the selector query and scans the result into the given value.
func (srs *ServiceRevisionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srs.ctx, "Select")
	if err := srs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServiceRevisionQuery, *ServiceRevisionSelect](ctx, srs.ServiceRevisionQuery, srs, srs.inters, v)
}

func (srs *ServiceRevisionSelect) sqlScan(ctx context.Context, root *ServiceRevisionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(srs.fns))
	for _, fn := range srs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*srs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (srs *ServiceRevisionSelect) Modify(modifiers ...func(s *sql.Selector)) *ServiceRevisionSelect {
	srs.modifiers = append(srs.modifiers, modifiers...)
	return srs
}
