// SPDX-FileCopyrightText: 2023 Seal, Inc
// SPDX-License-Identifier: Apache-2.0

// Code generated by "seal". DO NOT EDIT.

package model

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"

	"github.com/seal-io/seal/pkg/dao/model/service"
	"github.com/seal-io/seal/pkg/dao/model/servicerelationship"
	"github.com/seal-io/seal/pkg/dao/types/object"
	"github.com/seal-io/seal/utils/json"
)

// ServiceRelationship is the model entity for the ServiceRelationship schema.
type ServiceRelationship struct {
	config `json:"-"`
	// ID of the ent.
	ID object.ID `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime *time.Time `json:"create_time,omitempty"`
	// ID of the service that deploys after the dependency finished.
	ServiceID object.ID `json:"service_id,omitempty"`
	// ID of the service that deploys before the service begins.
	DependencyID object.ID `json:"dependency_id,omitempty"`
	// ID list of the service includes all dependencies and the service itself.
	Path []object.ID `json:"path,omitempty"`
	// Type of the relationship.
	Type string `json:"type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ServiceRelationshipQuery when eager-loading is set.
	Edges        ServiceRelationshipEdges `json:"edges,omitempty"`
	selectValues sql.SelectValues
}

// ServiceRelationshipEdges holds the relations/edges for other nodes in the graph.
type ServiceRelationshipEdges struct {
	// Service to which it currently belongs.
	Service *Service `json:"service,omitempty"`
	// Service to which the dependency belongs.
	Dependency *Service `json:"dependency,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ServiceOrErr returns the Service value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ServiceRelationshipEdges) ServiceOrErr() (*Service, error) {
	if e.loadedTypes[0] {
		if e.Service == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: service.Label}
		}
		return e.Service, nil
	}
	return nil, &NotLoadedError{edge: "service"}
}

// DependencyOrErr returns the Dependency value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ServiceRelationshipEdges) DependencyOrErr() (*Service, error) {
	if e.loadedTypes[1] {
		if e.Dependency == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: service.Label}
		}
		return e.Dependency, nil
	}
	return nil, &NotLoadedError{edge: "dependency"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ServiceRelationship) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case servicerelationship.FieldPath:
			values[i] = new([]byte)
		case servicerelationship.FieldID, servicerelationship.FieldServiceID, servicerelationship.FieldDependencyID:
			values[i] = new(object.ID)
		case servicerelationship.FieldType:
			values[i] = new(sql.NullString)
		case servicerelationship.FieldCreateTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ServiceRelationship fields.
func (sr *ServiceRelationship) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case servicerelationship.FieldID:
			if value, ok := values[i].(*object.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sr.ID = *value
			}
		case servicerelationship.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				sr.CreateTime = new(time.Time)
				*sr.CreateTime = value.Time
			}
		case servicerelationship.FieldServiceID:
			if value, ok := values[i].(*object.ID); !ok {
				return fmt.Errorf("unexpected type %T for field service_id", values[i])
			} else if value != nil {
				sr.ServiceID = *value
			}
		case servicerelationship.FieldDependencyID:
			if value, ok := values[i].(*object.ID); !ok {
				return fmt.Errorf("unexpected type %T for field dependency_id", values[i])
			} else if value != nil {
				sr.DependencyID = *value
			}
		case servicerelationship.FieldPath:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field path", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &sr.Path); err != nil {
					return fmt.Errorf("unmarshal field path: %w", err)
				}
			}
		case servicerelationship.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				sr.Type = value.String
			}
		default:
			sr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ServiceRelationship.
// This includes values selected through modifiers, order, etc.
func (sr *ServiceRelationship) Value(name string) (ent.Value, error) {
	return sr.selectValues.Get(name)
}

// QueryService queries the "service" edge of the ServiceRelationship entity.
func (sr *ServiceRelationship) QueryService() *ServiceQuery {
	return NewServiceRelationshipClient(sr.config).QueryService(sr)
}

// QueryDependency queries the "dependency" edge of the ServiceRelationship entity.
func (sr *ServiceRelationship) QueryDependency() *ServiceQuery {
	return NewServiceRelationshipClient(sr.config).QueryDependency(sr)
}

// Update returns a builder for updating this ServiceRelationship.
// Note that you need to call ServiceRelationship.Unwrap() before calling this method if this ServiceRelationship
// was returned from a transaction, and the transaction was committed or rolled back.
func (sr *ServiceRelationship) Update() *ServiceRelationshipUpdateOne {
	return NewServiceRelationshipClient(sr.config).UpdateOne(sr)
}

// Unwrap unwraps the ServiceRelationship entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sr *ServiceRelationship) Unwrap() *ServiceRelationship {
	_tx, ok := sr.config.driver.(*txDriver)
	if !ok {
		panic("model: ServiceRelationship is not a transactional entity")
	}
	sr.config.driver = _tx.drv
	return sr
}

// String implements the fmt.Stringer.
func (sr *ServiceRelationship) String() string {
	var builder strings.Builder
	builder.WriteString("ServiceRelationship(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sr.ID))
	if v := sr.CreateTime; v != nil {
		builder.WriteString("create_time=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("service_id=")
	builder.WriteString(fmt.Sprintf("%v", sr.ServiceID))
	builder.WriteString(", ")
	builder.WriteString("dependency_id=")
	builder.WriteString(fmt.Sprintf("%v", sr.DependencyID))
	builder.WriteString(", ")
	builder.WriteString("path=")
	builder.WriteString(fmt.Sprintf("%v", sr.Path))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(sr.Type)
	builder.WriteByte(')')
	return builder.String()
}

// ServiceRelationships is a parsable slice of ServiceRelationship.
type ServiceRelationships []*ServiceRelationship
