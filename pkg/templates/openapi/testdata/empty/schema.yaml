openapi: 3.0.3
info:
  title: OpenAPI schema for template empty
components:
  schemas:
    variables:
      type: object
      required:
      - bool
      - num
      - str
      properties:
        str:
          title: Str
          type: string
          x-walrus-ui:
            order: 1
        num:
          title: Num
          type: number
          x-walrus-ui:
            order: 2
        bool:
          title: Bool
          type: boolean
          x-walrus-ui:
            order: 3
